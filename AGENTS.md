# AGENTS.md

## Documentation Rule

Whenever you change this NvChad config, update the relevant files in `docs/` in the same change set.

## Scope

- Keep documentation focused on local overrides on top of base NvChad.
- Prefer updating `docs/customizations.md` for behavior changes, plugins, mappings, options, or language tooling tweaks.
- Update `README.md` too when the repo structure or onboarding notes change.
