# NvChad Customizations

This folder tracks the changes made on top of base NvChad so the repo stays easy to reason about.

- [customizations.md](./customizations.md): current overrides, plugin additions, and behavior changes.

When the config changes, update the relevant docs here in the same commit.
